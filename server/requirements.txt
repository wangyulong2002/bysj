# Web 框架
fastapi>=0.100
uvicorn[standard]>=0.23
pydantic>=2.5
pydantic-settings>=2.1

# 数据库
SQLAlchemy>=2.0
PyMySQL>=1.1
cryptography>=41.0

# Redis（缓存+向量+限流）
redis>=5.0

# ===== RAG（T7-1，8.2 技术底座）=====
openai>=1.50          # 方舟 OpenAI 兼容接口（生成+Embedding+Agnes 兜底）
langchain>=1.0        # RAG 编排（自定义 Retriever，不用内置 VectorStore 抽象）
langchain-openai>=1.0 # ChatOpenAI 生成端入口
numpy>=2.0            # 2560 维 float32 序列化
APScheduler>=3.10     # Worker 定时调度（30s 一轮 + 日志清理）

# JWT
PyJWT>=2.8

# 工具
python-multipart>=0.0.6
python-dotenv>=1.0

# 测试（T0-6/T0-7 单元测试）
pytest>=8.0
httpx>=0.27

# ===== 管理端（T0-1/T0-2，Django 5.2 LTS 对齐 Python 3.14，A-01）=====
Django>=5.2,<5.3
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
